name: Build and Sign Android App (AAB & APK)

on:
  workflow_dispatch: # Allows manual trigger from GitHub Actions tab
  push:
    branches:
      - main
      - master

jobs:
  build-and-sign:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Install Dependencies
        run: |
          npm install -g @ionic/cli
          npm install --legacy-peer-deps

      - name: Build Web Assets
        run: npm run build -- --configuration=production

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'zulu'
          cache: 'gradle'

      - name: Set up Android SDK Build Tools
        uses: android-actions/setup-android@v3

      - name: Sync Capacitor Android Project
        run: npx cap sync android

      - name: Build Android Release (AAB & APK)
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleRelease bundleRelease --no-daemon

      - name: Prepare Keystore File
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
        run: |
          if [ -n "$KEYSTORE_BASE64" ]; then
            echo "Decoding keystore from GitHub Secret..."
            echo "$KEYSTORE_BASE64" | base64 --decode > pintu-partner.keystore
          elif [ -f "pintu-partner.keystore" ]; then
            echo "Using existing repository keystore: pintu-partner.keystore"
          else
            echo "❌ Error: Keystore not found in repository or secrets!"
            exit 1
          fi

      - name: Sign Android App Bundle (.aab) & APK (.apk)
        env:
          KEYSTORE_PATH: pintu-partner.keystore
          KEY_ALIAS: ${{ secrets.KEY_ALIAS || 'my-key-alias' }}
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD || secrets.KEYSTORE_PASSWORD }}
        run: |
          BUILD_TOOLS_DIR=$(ls -d $ANDROID_HOME/build-tools/* | sort -V | tail -n 1)
          echo "Using Android Build Tools from: $BUILD_TOOLS_DIR"

          AAB_FILE="android/app/build/outputs/bundle/release/app-release.aab"
          AAB_SIGNED="android/app/build/outputs/bundle/release/pintu-partner-release-signed.aab"
          UNSIGNED_APK="android/app/build/outputs/apk/release/app-release-unsigned.apk"
          FINAL_UNSIGNED_APK="android/app/build/outputs/apk/release/pintu-partner-release-unsigned.apk"
          ALIGNED_APK="android/app/build/outputs/apk/release/app-release-aligned.apk"
          SIGNED_APK="android/app/build/outputs/apk/release/pintu-partner-release-signed.apk"

          echo "🔐 Signing .aab bundle with jarsigner..."
          if [ -n "$KEYSTORE_PASSWORD" ]; then
            jarsigner -verbose -sigalg SHA256withRSA -digestalg SHA-256 \
              -keystore "$KEYSTORE_PATH" \
              -storepass "$KEYSTORE_PASSWORD" \
              -keypass "$KEY_PASSWORD" \
              "$AAB_FILE" "$KEY_ALIAS"
          else
            jarsigner -verbose -sigalg SHA256withRSA -digestalg SHA-256 \
              -keystore "$KEYSTORE_PATH" \
              "$AAB_FILE" "$KEY_ALIAS"
          fi
          cp "$AAB_FILE" "$AAB_SIGNED"

          echo "📦 Aligning and Signing .apk with apksigner..."
          if [ -f "$UNSIGNED_APK" ]; then
            cp "$UNSIGNED_APK" "$FINAL_UNSIGNED_APK"
            $BUILD_TOOLS_DIR/zipalign -v -p 4 "$UNSIGNED_APK" "$ALIGNED_APK" || cp "$UNSIGNED_APK" "$ALIGNED_APK"
            
            if [ -n "$KEYSTORE_PASSWORD" ]; then
              $BUILD_TOOLS_DIR/apksigner sign \
                --ks "$KEYSTORE_PATH" \
                --ks-pass "pass:$KEYSTORE_PASSWORD" \
                --ks-key-alias "$KEY_ALIAS" \
                --key-pass "pass:$KEY_PASSWORD" \
                --out "$SIGNED_APK" \
                "$ALIGNED_APK"
            else
              $BUILD_TOOLS_DIR/apksigner sign \
                --ks "$KEYSTORE_PATH" \
                --ks-key-alias "$KEY_ALIAS" \
                --out "$SIGNED_APK" \
                "$ALIGNED_APK"
            fi

            echo "✅ Verifying signed APK..."
            $BUILD_TOOLS_DIR/apksigner verify "$SIGNED_APK"
          else
            echo "⚠️ Unsigned APK not found at $UNSIGNED_APK"
          fi

      - name: Upload Signed .APK (Ready for Direct Install)
        uses: actions/upload-artifact@v4
        with:
          name: pintu-partner-signed-apk
          path: android/app/build/outputs/apk/release/pintu-partner-release-signed.apk
          retention-days: 14

      - name: Upload Unsigned .APK
        uses: actions/upload-artifact@v4
        with:
          name: pintu-partner-unsigned-apk
          path: android/app/build/outputs/apk/release/pintu-partner-release-unsigned.apk
          retention-days: 14

      - name: Upload Signed .AAB (Google Play Store Bundle)
        uses: actions/upload-artifact@v4
        with:
          name: pintu-partner-signed-aab
          path: android/app/build/outputs/bundle/release/pintu-partner-release-signed.aab
          retention-days: 14
